[project]
name = "btm-topographic-adjustment-lambda"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["btm-topographic-adjustment-core"]

[tool.uv.sources]
btm-topographic-adjustment-core = { path = "../../python/topographic-adjustment-core" }
