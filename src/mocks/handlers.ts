import type { HttpHandler } from 'msw';

/**
 * Populated in T01.9 (repositories, DemoRepository, MSW and persistence). Kept empty here so
 * the MSW worker/server can be wired end-to-end in T01.1 without inventing API behaviour.
 */
export const handlers: HttpHandler[] = [];
